
{
  "name": "ziptitlepro",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "echo 'Running in development mode with Play CDN...'",
    "build": "mkdir -p dist && cp -rv index.html index.tsx App.tsx types.ts metadata.json components services dist/"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.1",
    "postcss": "^8.4.35",
    "autoprefixer": "^10.4.18"
  }
}
